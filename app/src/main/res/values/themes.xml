<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RoomUserInfo" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="windowActionModeOverlay">false</item>
        <item name="android:windowTranslucentStatus">true</item>
        
    </style>

    <style name="CloseButton" parent="Widget.AppCompat.ActionButton.CloseMode">
        <item name="android:visibility">gone</item>
    </style>
    <style name="actionBarStyle" parent="ThemeOverlay.AppCompat.DayNight.ActionBar">
        <item name="android:actionModeCloseButtonStyle">@style/CloseButton</item>
        <item name="windowActionModeOverlay">false</item>
    </style>

    <style name="toolbarStyle" parent="Theme.RoomUserInfo">
        <item name="android:actionModeCloseButtonStyle">@style/CloseButton</item>
        <item name="windowActionModeOverlay">true</item>
    </style>
</resources>